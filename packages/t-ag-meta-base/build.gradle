plugins {
    id "nebula.ospackage" version "4.10.0"
}

version = "1.1"
description = "Ein Metapaket für Grundlegende Konfigurationen"

// The distribution package
task getPackage(type: Deb) {
    group = "distribution"

    packageName = "t-ag-meta-base"
    maintainer = "Technik-AG"
    release = "1"
    user = "root"

    // Internal packages
    requires("t-ag-auto-upgrade")
    requires("t-ag-motd-banner")
    requires("t-ag-sudo-pwfeedback")

    // External packages
    requires("avahi-daemon")
}